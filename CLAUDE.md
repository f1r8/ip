@AGENTS.md

After every code update, follow the code-update verification workflow in `AGENTS.md`: review or update `test/ui-test-plan.md` as needed, invoke `$test-ui`, and show the resulting console session. Stop and report actual versus expected output on the first failure.
